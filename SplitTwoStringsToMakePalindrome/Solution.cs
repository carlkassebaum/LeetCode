using System;
using System.Collections.Generic;
using System.Text;

namespace SplitTwoStringsToMakePalindrome
{
    public class Solution
    {
        public bool CheckPalindromeFormation(string a, string b)
        {
            var aPrefix = "";
            var aSuffix = a;

            var bPrefix = "";
            var bSuffix = b;

            //

            for (int i = 0; i < a.Length; i++)
            {

            }



            // Really easy? Just maintain the two substrings. 
            // And as you go, add or remove characters appropriately. 



        }
    }
}
